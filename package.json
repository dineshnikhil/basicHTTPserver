{
	"name": "basichttpserver",
	"version": "1.0.0",
	"description": "created the basic http server with out using external packages only using the internal node packages like http.",
	"main": "index.js",
	"scripts": {
		"start": "nodemon index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"body-parser": "^1.20.2",
		"express": "^4.18.2",
		"uuid": "^9.0.0"
	}
}
